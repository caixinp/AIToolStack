[project]
name = "aitoolstack"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "backend",
]

[tool.uv.workspace]
members = [
    "backend",
]

[tool.uv.sources]
backend = { workspace = true }
